# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: groknett-valueforge
metadata:
  template: groknett-valueforge@1.0.0

# Service definitions
services:
  app:
    project: .
    language: js
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .

# Deployment pipeline
pipeline:
  provider: github
